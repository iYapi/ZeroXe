[project]
name = "zeroxe"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "iYapi", email = "yafialhaidar18@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
zeroxe = "zeroxe:main"

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"
